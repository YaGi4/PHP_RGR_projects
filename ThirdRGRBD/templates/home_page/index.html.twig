<!DOCTYPE html>
<html lang="en">
    <head>
        <title>{{ page_title }}</title>
        <link href="{{ absolute_url(asset('styles/home.css') )}}" rel="stylesheet">
    </head>
    <body class="body">
    {% include 'blocks/LeftPanel.html.twig'%}

    {% if user_name == null %}
        <div class="welcomeLabel">Welcome to Home page</div>
    {% else %}
        <div class="welcomeLabel">{{ user_name }}, welcome to Home page</div>
    {% endif %}


    </body>
</html>